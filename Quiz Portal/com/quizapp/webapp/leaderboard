<%@ taglib uri="http://java.sun.com/jsp/jstl/core" prefix="c"%>
<html><head><title>Leaderboard</title></head>
<body>
<div class="container">
  <h3>Leaderboard</h3>
  <table class="table">
    <thead><tr><th>#</th><th>User</th><th>Score</th><th>Date</th></tr></thead>
    <tbody>
      <c:forEach var="a" items="${attempts}" varStatus="st">
        <tr>
          <td>${st.index + 1}</td>
          <td>${names[st.index]}</td>
          <td>${a.score}/${a.totalQuestions}</td>
          <td>${a.createdAt}</td>
        </tr>
      </c:forEach>
    </tbody>
  </table>
</div>
</body></html>
